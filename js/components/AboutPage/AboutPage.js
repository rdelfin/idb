// @flow
import React, {PureComponent} from 'react';
import PlaceholderPage from '../PlaceholderPage';

export default class AboutPage extends PureComponent {
  render() {
    return (
      <PlaceholderPage>AboutPage</PlaceholderPage>
    );
  }
}
